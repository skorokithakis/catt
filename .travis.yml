language: python

cache: pip

python:
  - "3.4"
  - "3.5"
  - "3.6"

install: "pip install -r requirements_dev.txt"

script:
  # Only run this on Python 3.6.
  - command -v python3.6 && pre-commit run -a
  - python setup.py test
